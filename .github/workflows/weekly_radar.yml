name: Weekly Academic Radar Scan

on:
  # Run every Monday at 9:00 UTC
  schedule:
    - cron: '0 9 * * 1'
  
  # Allow manual triggering
  workflow_dispatch:
    inputs:
      mode:
        description: 'Execution mode'
        required: false
        default: 'search'
        type: choice
        options:
          - search
          - profile
          - full

# Required for workflow to push changes back to repo
permissions:
  contents: write

jobs:
  run-radar:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          # Fetch full history to access cached profile.json
          fetch-depth: 0
      
      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🔍 Check for cached profile
        id: check-profile
        run: |
          if [ -f "cache/profile.json" ]; then
            echo "profile_exists=true" >> $GITHUB_OUTPUT
            echo "✅ Found cached research profile"
          else
            echo "profile_exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ No cached profile found - will need to run profiler first"
          fi
      
      - name: ⚙️ Determine execution mode
        id: set-mode
        run: |
          if [ "${{ github.event.inputs.mode }}" != "" ]; then
            MODE="${{ github.event.inputs.mode }}"
          elif [ "${{ steps.check-profile.outputs.profile_exists }}" == "false" ]; then
            MODE="full"
          else
            MODE="search"
          fi
          echo "mode=$MODE" >> $GITHUB_OUTPUT
          echo "🎯 Execution mode: $MODE"
      
      - name: 🚀 Run Academic Radar
        env:
          # LLM API Keys (configure in GitHub Secrets)
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          
          # OpenAlex Configuration
          OPENALEX_EMAIL: ${{ secrets.OPENALEX_EMAIL }}
          OPENALEX_API_KEY: ${{ secrets.OPENALEX_API_KEY }}
          
          # SMTP Configuration
          SMTP_HOST: ${{ secrets.SMTP_HOST || 'smtp.gmail.com' }}
          SMTP_PORT: ${{ secrets.SMTP_PORT || '587' }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
          
          # System Configuration
          PROFILE_CACHE_PATH: ./cache/profile.json
          USER_PAPERS_PATH: ./data/user_papers
          
          # Search & Analysis Parameters (edit these values directly in this file)
          MAX_PAPERS_TO_ANALYZE: 10 # Number of papers to deeply analyze per search
          SEARCH_DAYS_BACK: 600 ## How many days back to search for papers
          MIN_BORROWABILITY_SCORE: 0.5 # Minimum score (0.0-1.0) to include paper in results
          MAX_RESULTS_PER_QUERY: 20 # Maximum papers to fetch per query
          
          # Deep Search Configuration
          TARGET_NEW_PAPERS: 10 # Target number of new papers to find per run
          MAX_PAGES_TO_SCAN: 5 # Maximum pages to scan per query
          
          # Paper Tracking
          PAPER_HISTORY_DAYS: 365 # How long to remember sent papers
          
          # LLM Configuration
          LLM_TEMPERATURE: 0.7 # LLM creativity (0.0-1.0)
          LLM_MODEL: ${{ secrets.LLM_MODEL || 'deepseek-reasoner' }}  # LLM model to use
        
        run: |
          echo "🎯 Starting Academic Radar in '${{ steps.set-mode.outputs.mode }}' mode"
          python main.py --mode ${{ steps.set-mode.outputs.mode }}
      
      - name: 💾 Commit updated cache files
        if: success()
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "Academic Radar Bot"
          
          # Add all cache files
          git add cache/sent_papers.json 2>/dev/null || true
          git add cache/profile.json 2>/dev/null || true
          
          # Only commit if there are changes
          if git diff --staged --quiet; then
            echo "ℹ️ No changes to cache files"
          else
            git commit -m "🤖 Update cache files [skip ci]"
            git push
            echo "✅ Cache files updated and committed"
          fi
      
      - name: 📊 Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: radar-logs
          path: |
            academic_radar.log
            cache/profile.json
          retention-days: 30
      
      - name: 📢 Notify on failure
        if: failure()
        run: |
          echo "❌ Academic Radar failed. Check the logs for details."
          # Optional: Add Slack/Discord notification here
